# Review package: 196afa9..72b209f

## Commits
72b209f fix(ui): guard players_menu players_changed connection against duplicate setup

## Files changed
 Game/source/scripts/ui/players_menu.gd | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

## Diff
diff --git a/Game/source/scripts/ui/players_menu.gd b/Game/source/scripts/ui/players_menu.gd
index 891f08f..ca2afbb 100644
--- a/Game/source/scripts/ui/players_menu.gd
+++ b/Game/source/scripts/ui/players_menu.gd
@@ -21,21 +21,22 @@ var _preview_avatar: Node3D = null
 func _ready() -> void:
 	visible = false
 	_apply_styling()
 	_build_action_buttons()
 
 func setup(lan_ref: Node, compositor_ref: Node, file_share_ref: Node) -> void:
 	_lan = lan_ref
 	_compositor = compositor_ref
 	_file_share = file_share_ref
 	if _lan != null and _lan.has_signal("players_changed"):
-		_lan.players_changed.connect(_refresh_tabs)
+		if not _lan.players_changed.is_connected(_refresh_tabs):
+			_lan.players_changed.connect(_refresh_tabs)
 
 func _apply_styling() -> void:
 	var bg := StyleBoxFlat.new()
 	bg.bg_color = Color(0.06, 0.06, 0.08, 0.95)
 	bg.border_color = Color(0.3, 0.4, 0.6, 0.8)
 	bg.border_width_top = 1
 	bg.border_width_bottom = 1
 	bg.border_width_left = 1
 	bg.border_width_right = 1
 	bg.corner_radius_top_left = 10
